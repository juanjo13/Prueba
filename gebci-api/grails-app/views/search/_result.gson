import mx.gob.fgjez.gebci.search.SearchResult

model {
	SearchResult result
}

json {
	id result.carpeta.id
	numero result.carpeta.numero
	fechaInicio result.carpeta.fechaInicio
	descripcion result.carpeta.descripcion
	procedencia result.carpeta.procedencia.descripcion
	estatus result.carpeta.estatus
	coincidencia g.render(result.coincidencia)
}

